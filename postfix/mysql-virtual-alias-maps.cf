user = {{DB_USER}}
password = {{DB_PASSWORD}}
hosts = {{DB_HOST}}
dbname = {{DB_NAME}}
query = SELECT destination FROM mail_view_aliases WHERE email='%s' UNION ALL SELECT destination FROM mail_view_aliases WHERE email='@%d' AND NOT EXISTS (SELECT destination FROM mail_view_aliases WHERE email='%s')
